const {resolve} = require('path');

module.exports = {
    config:resolve(_dirname,'src', 'configs', 'db.js'),
    'models-path':resolve(_dirname, 'src', 'apps', 'models'),
    'migrations-path':resolve(_dirname, 'src', 'database', 'migrations'),
};